import { HeightTransitionDirective } from './height-transition.directive';

describe('HeightTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
